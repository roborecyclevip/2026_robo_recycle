SERIAL_PORT = "/dev/ttyACM0" 
# Try "/dev/ttyACM1" if it's not working, it wouldn't work if
# you had an existing serial and then disconnected stuff while
# it was open, so it might just jump to ACM1 seeing that 
# ACM0 was used
BAUD_RATE = 115200
